enum Action {
  Read = 'read',
  Create = 'create',
  Update = 'update',
  Delete = 'delete',
}

const ACTIONS = [Action.Read, Action.Create, Action.Update, Action.Delete];

export { Action, ACTIONS };
